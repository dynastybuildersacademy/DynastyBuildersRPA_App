
name: call_tracker_plus
description: A personal assistant app for insurance agents.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^19.3.1
  google_sign_in: ^7.1.1
  table_calendar: ^3.1.0
  http: ^1.4.0
  intl: 0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
